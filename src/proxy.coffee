http = require 'http'

class Proxy
    port: 80
    host: 'local'

    _nodes: []

    constructor: ->

    startServer: ->
    stopServer: ->

    addNode: (name, port)->

    removeNode: (name)->


module.exports = Proxy
