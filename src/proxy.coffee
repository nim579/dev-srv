http = require 'http'
_ = require 'underscore'

class Proxy
    _nodes: []

    constructor: ->

    startServer: ->


    stopServer: ->

    addNode: (name, port)->


    removeNode: (name)->


module.exports = Proxy
